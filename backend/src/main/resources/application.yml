spring:
  application:
    name: helpclub-backend
server:
  port: 8080

ollama:
  base-url: ${OLLAMA_BASE_URL:http://localhost:11434}
  model: ${OLLAMA_MODEL:llama3.1}

management:
  endpoints:
    web:
      exposure:
        include: health,info
